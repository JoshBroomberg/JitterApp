<%= form_for @profile do |f| %>

<%= f.label :name %>(Enter a display name)<br>
<%= f.text_field :name %>
<br><br>

<%= f.label :description %>(Enter a description)<br>
<%= f.text_area :description %>

<%= f.submit %>

<% end %>